"""Local web editor over the dubbing pipeline's run directories.

`editor.app` is a FastAPI app; `editor.edits` holds the manifest-patching logic
(pure, unit-tested in `tests/test_editor_edits.py`).
"""

{
  "name": "dubbing-studio-desktop",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "pretauri": "uv run --script ../../scripts/build_desktop.py check && uv run --script ../../scripts/stage_desktop_payload.py",
    "tauri": "tauri",
    "dev": "pnpm --dir ../ui dev",
    "build": "uv run --script ../../scripts/build_desktop.py ui"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-http": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-store": "^2"
  }
}

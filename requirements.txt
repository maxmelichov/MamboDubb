# Legacy pip lists — prefer `uv sync` / `pyproject.toml`.
#
# Kept as a readable map of which packages belong to which stage:
#   uv pip install -r requirements/translate.txt
#   uv pip install -r requirements/transcribe.txt
#   uv pip install -r requirements/extract.txt

-r requirements/translate.txt
-r requirements/transcribe.txt
-r requirements/extract.txt
